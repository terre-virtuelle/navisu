dependencies {
    compile project(':navisu-charts')
    compile project(':navisu-dem')
    compile project(':navisu-geometry')
    compile fileTree(dir: 'lib', include: '*.jar') 
}
jar {
   destinationDir project.file('../dest')
}
